# Login UI

A simple, responsive **Login** user interface built with HTML and CSS.  
Frontend-only demo (no backend). Perfect for practicing forms, layout, and basic client-side validation.

## 🔗 Live
(When deployed with GitHub Pages, link will be:)
`https://NaveenLikesCoding.github.io/login-ui`

## 🧩 Features
- Clean, centered login form
- Client-side validation (email + min password length)
- Accessible labels and ARIA status for messages
- Responsive for mobile and desktop
- Simple link back to portfolio

## 📁 Project structure
index.html ← Login page (entry)
style.css ← Styles for the page
images/ ← Background or assets (if any)

markdown
Copy code

## 🖼 Screenshot
Local path of the screenshot you provided:
`/mnt/data/A_digital_screenshot_displays_a_simple_personal_po.png`

(You can add this image to the `images/` folder and reference it in the README later.)

## 🚀 How to run locally
1. Open the project folder in VS Code.  
2. Open `index.html` and run **Live Server** (right-click → Open with Live Server).  
3. Or open `index.html` directly in your browser.

## ✅ Notes
- This project is frontend-only — no user data is stored or sent anywhere.  
- To add a signup page later, create `signup.html` and link from the login page.  
- For a live demo, upload the files to a GitHub repo and enable GitHub Pages (branch `main`, folder `/ (root)`).

## 📝 Commit message suggestion
`Create README.md`

## Author
Naveen — learning web development.
